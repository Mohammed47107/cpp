git add . (Preparas los cambios).

git commit -m "mensaje" (Le pones nombre a lo que hiciste).

git push (Lo envías a la nube).

siempre para ejecutar codigo se usará:
g++ {nombre del archivo.c} -o {nombre del archivo ejecutable}
.\{nombre del archivo ejecutable.exe}